build:
  environment:
    php: '5.5.25'
  dependencies:
    before:
      - composer install
  tests:
    override:
      -
        command: 'phpunit --coverage-clover=.coverage'
        coverage:
            file: '.coverage'
            format: 'clover'
